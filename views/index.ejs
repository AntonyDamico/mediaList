<%- include partials/header.ejs %>

<link rel="stylesheet" type="text/css" href="css/index-style.css">
<link href="https://fonts.googleapis.com/css?family=Bevan&display=swap" rel="stylesheet">
<link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.8.2/css/all.css" integrity="sha384-oS3vJWv+0UjzBfQzYUhtDYW+Pj2yciDJxpsK1OYPAYjqT085Qq/1cq5FLXAZQ7Ay" crossorigin="anonymous">


<%- include partials/navbar-loged.ejs %>
<div class="iniciar-menu-container">
    <div class="iniciar-menu" id="menu-login" style="left: 100%;">
        <p class="menu-login-title">Iniciar Sesión</p>
        <form action="/auth/login" method="POST">
            <input type="username" name="username" placeholder="Nombre de Usuario:">
            <input type="password" name="password" placeholder="Contraseña:">
            <input type="submit" class="submit-button" name="submit-newuser" value="Continuar">
        </form>
    </div>
</div>
<div class="iniciar-menu-container">
    <div class="iniciar-menu" id="menu-register" style="left: 100%;">
        <p class="menu-login-title">Registrarse</p>
        <form action="/auth/register" method="POST">
            <input type="text" name="username" placeholder="Nombre:">
            <input type="email" name="email" placeholder="Correo:">
            <input type="password" name="password" placeholder="Contraseña:">
            <input type="password" name="confirmPassword" placeholder="Confirmar contraseña:">
            <input type="submit" class="submit-button" name="submit-newuser" value="Continuar">
        </form>
    </div>
</div>

<div class="base-container">

    <%- include partials/sidebar.ejs %>

    <div class="content">
        <p class="title-content">Series:</p>
        <div class="movies-container">
            <a class="movies-move-icon"><img src="media/icons/Recurso%203.png"></a>
            <% showData.forEach(show =>{ %>
                <a href="/media/show/<%= show.id %>" class="movie-item">
                    <img src="<%= show.poster %>">
                    <i class="far fa-heart icon-fav"></i>

                    <!--<i class="fas fa-heart icon-fav"></i>-->
                    
                    <div class="movie-item-text-container">
                        <p class="movie-item-text"><%= show.title %><br> <%= show.release_date.getFullYear() %></p>
                    </div>
                </a>
            <% }) %>
            <a class="movies-move-icon"><img src="media/icons/Recurso%202.png"></a>
        </div>


        <p class="title-content">Tendencias:</p>
        <div class="movies-container">
            <a class="movies-move-icon"><img src="media/icons/Recurso%203.png"></a>
            <% movieData.forEach(movie =>{ %>
                <a href="/media/movie/<%= movie.id %>" class="movie-item">
                    <img src="<%= movie.poster %>">
                    <i class="far fa-heart icon-fav"></i>
                    <!--Selecionado
                    <i class="fas fa-heart icon-fav"></i>
                    -->
                    <div class="movie-item-text-container">
                        <p class="movie-item-text"><%= movie.title %><br> <%= movie.release_date.getFullYear() %></p>
                    </div>
                </a>
            <% }) %>
            <a class="movies-move-icon"><img src="media/icons/Recurso%202.png"></a>
        </div>


        <p class="title-content">Tendencias:</p>
        <div class="movies-container">
            <a class="movies-move-icon"><img src="media/icons/Recurso%203.png"></a>
            <% movieData.forEach(movie =>{ %>
                <a href="/media/movie/<%= movie.id %>" class="movie-item">
                    <img src="<%= movie.poster %>">
                    <i class="far fa-heart icon-fav"></i>
                    <!--Selecionado
                    <i class="fas fa-heart icon-fav"></i>
                    -->
                    <div class="movie-item-text-container">
                    <p class="movie-item-text"><%= movie.title %><br> <%= movie.release_date.getFullYear() %></p>
                </div>
                </a>
            <% }) %>
            <a class="movies-move-icon"><img src="media/icons/Recurso%202.png"></a>
        </div>

    </div>
</div>


<div class="information-container">

</div>
<script>
    var buttonL = document.getElementById("SignIn");
    var buttonR = document.getElementById("SignUp");
    var menuL = document.getElementById("menu-login");
    var menuR = document.getElementById("menu-register");

    buttonL.addEventListener("click", function() {
    if (menuL.style.left === "100%") {
      menuL.style.left = "0%";
      menuL.style.zIndex = "10";
    } else {
      menuL.style.left = "100%";
      menuL.style.zIndex = "0";
    }
    if (menuR.style.left === "0%") {
      menuR.style.left = "100%";
      menuR.style.zIndex = "0";
    }
    });

    buttonR.addEventListener("click", function() {
    if (menuR.style.left === "100%") {
      menuR.style.left = "0%";
      menuR.style.zIndex = "10";
    } else {
      menuR.style.left = "100%";
      menuR.style.zIndex = "0";
    }
    if (menuL.style.left === "0%") {
      menuL.style.left = "100%";
      menuL.style.zIndex = "0";
    }
    });

</script>

<script>
    var panels = document.getElementsByClassName("panel-button");

    for(var i=0; i<panels.length; i++){
        panels[i].addEventListener("click", function(){
            var content = this.nextElementSibling;
            if (content.style.display === "none") {
                content.style.display = "block";
            } else {
                content.style.display = "none";
            }
        });
    }
</script>
<%- include partials/footerBar.ejs %>
<%- include partials/footer.ejs %>
